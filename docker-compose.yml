version: '3.8'

services:
  neuralswarm:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: neuralswarm-coordinator
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
